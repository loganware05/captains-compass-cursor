Captain's Compass **v1.15.0** — Fixture embeddings and package-registry file TI (Milestone 11).

### Added
- Fixture EmbeddingProvider + dense embedding index (TF-IDF always fallback)
- Package-registry file TI (`COMPASS_TI_PROVIDER=package-registry-file`)
- Skills `embedding-providers` + `package-registry-ti` (38 Skills)
- ADR-027

### Upgrade
```bash
./scripts/update.sh /path/to/product-repo
```

See CHANGELOG.md for full notes.
