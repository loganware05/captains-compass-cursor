Captain's Compass **v1.16.0** — Live embeddings, package-registry TI, soft-hook skip-env (Milestone 12).

### Added
- OpenAI-compatible embedding provider (`COMPASS_EMBEDDING_*`)
- Live package-registry TI for npm + PyPI
- Soft-hook `.agent/compass-skip.env` inheritance
- ADR-028

### Upgrade
```bash
./scripts/update.sh /path/to/product-repo
```

See CHANGELOG.md for full notes.
