Follow Captain's Compass product onboarding for this repository.

1. Read `AGENTS.md`, `PROJECT_CONTEXT.md`, `DECISIONS.md`, `PROGRESS.md`, and `TESTING.md`.
2. Inspect Git status and the active branch (do not implement on a protected base branch).
3. If Compass workflow files are missing, tell the Captain to run install from the control repo.
4. Interview the Captain only as needed to fill gaps in `PROJECT_CONTEXT.md` (product summary, stack, constraints).
5. Do **not** modify product implementation files.
6. Leave `IMPLEMENTATION_PLAN.md` as DRAFT or untouched unless the Captain asks to plan a feature.
7. Reply with: current Compass version (`.agent/COMPASS_VERSION` if present), what is ready, and the recommended next slash command (`/plan-feature`).

Additional context from the Captain (if any) follows this command.
