# Python / ML (V0.6)

## Skill

- `.cursor/skills/python-ml/SKILL.md`

## Example fixture

See `examples/python-ml/` for a minimal illustrative layout (not a full training stack).

## Safety

- Keep large datasets and model weights out of git (see `.cursorignore` / `.gitignore`)
- Prefer development datasets during planning
- Record experiment config for reproducibility
