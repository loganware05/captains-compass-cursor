{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://captains-compass.dev/schemas/routing-proposal.schema.json",
  "title": "RoutingProposal",
  "description": "Experience-based routing proposal. auto_apply stays false; Captain-flagged apply via apply-routing-proposal.sh (Milestone 4).",
  "type": "object",
  "required": [
    "proposal_id",
    "kind",
    "based_on_experiences",
    "skill_confidence_deltas",
    "matcher_weight_suggestions",
    "auto_apply"
  ],
  "additionalProperties": true,
  "properties": {
    "proposal_id": { "type": "string", "minLength": 1 },
    "kind": { "type": "string", "const": "routing-proposal" },
    "based_on_experiences": {
      "type": "array",
      "items": { "type": "string" }
    },
    "skill_confidence_deltas": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["skill_id", "delta", "rationale"],
        "properties": {
          "skill_id": { "type": "string" },
          "delta": { "type": "number" },
          "rationale": { "type": "string" }
        },
        "additionalProperties": true
      }
    },
    "matcher_weight_suggestions": {
      "type": "object",
      "description": "Suggested WEIGHTS map; applied only via Captain-flagged CLI",
      "additionalProperties": { "type": "number" }
    },
    "auto_apply": { "type": "boolean", "const": false },
    "captain_approved": {
      "type": "boolean",
      "description": "Must be true before apply-routing-proposal.sh will apply weights"
    },
    "notes": { "type": "string" },
    "created_at": { "type": "string" , "minLength": 1 },
    "captain_approval_required": { "type": "boolean", "const": true }
  }
}
