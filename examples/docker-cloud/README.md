# Example: Docker / cloud preview fixture

Illustrative Compose layout for Captain's Compass docs. Not a production stack.

```bash
# from a product repo that adopts this pattern
docker compose -f examples/docker-cloud/compose.yaml config
```

Use the `docker-cloud` Skill after plan approval.
