# Example: iOS fixture

Illustrative placeholder for Captain's Compass docs.

A real product repo would contain an `.xcodeproj` / `.xcworkspace` and Swift sources.
Use the `ios-engineering` Skill after plan approval.
