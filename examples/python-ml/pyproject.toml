[project]
name = "compass-python-ml-fixture"
version = "0.0.0"
description = "Illustrative Python/ML fixture for Captain's Compass"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
