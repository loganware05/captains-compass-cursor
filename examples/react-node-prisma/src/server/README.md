# Server placeholder

In a real product repository, Node API handlers would live here (or under the project's existing server layout).

Use the `node-engineering` and `postgres-prisma` Skills after plan approval.
